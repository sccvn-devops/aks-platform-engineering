apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name }}
  namespace: kube-system
  labels:
    app.kubernetes.io/name: controller-scaler
    app.kubernetes.io/component: controller
