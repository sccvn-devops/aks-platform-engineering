apiVersion: v1
kind: ServiceAccount
metadata:
  name: saas-token-rotator
  namespace: {{ .Release.Namespace }}
  annotations:
    azure.workload.identity/client-id: {{ .Values.identityClientId | quote }}
  labels:
    azure.workload.identity/use: "true"
