apiVersion: v1
kind: ConfigMap
metadata:
  name: myapp-runtime-config
  namespace: placeholder-workload
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  labels:
    app.kubernetes.io/part-of: myapp
data:
  environment: placeholder
  service: myapp
  tier: workload
